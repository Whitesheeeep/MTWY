namespace FarmSystem
{
    /// <summary>
    /// 旧版集中规则管线的动作类型。当前门面已改用三个独立规则管线模块。
    /// </summary>
    public enum FarmActionType
    {
        Till,
        Water,
        Plant,
        Harvest
    }
}
